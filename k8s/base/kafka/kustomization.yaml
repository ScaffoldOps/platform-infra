apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: scaffoldops-dev

resources:
  - kafka-pvc.yaml
  - kafka-deployment.yaml
  - kafka-service.yaml
  - kafka-ui-deployment.yaml
  - kafka-ui-service.yaml
  - kafka-topic-bootstrap-job.yaml
