apiVersion: v1
kind: Service
metadata:
  name: keycloak-pre
  namespace: security
spec:
  type: ClusterIP
  selector:
    app: keycloak-pre
  ports:
    - name: http
      port: 8080
      targetPort: 8080
